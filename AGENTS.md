# 项目协作约定

- UI 设计参考统一放在仓库根目录的 `参考/`。修改控制台页面时，先查看相关样板图片，再检查实际页面在浏览器中的效果。
- 样板用于视觉层级、布局和交互方式参考；图中的任务数、质量分、日期与文件名是示意数据。页面必须展示真实工作区状态，不得把示意数据写入产品逻辑。
- 主要视觉语言是浅蓝白色工作台、紧凑侧栏、页首横幅、清晰卡片、阶段进度和可读的样本预览。生成、审核、打包页之间保持一致的术语与状态。
